<br>
<div class="container">
  <div class="row">
      <div class="button-box">
        <% if policy(@ingredient).new? %>
          <%= link_to raw('<button type="button" class="btn btn-primary">Criar novo ingrediente</button>'), new_ingredient_path(@ingredient) %>
        <% end %>
      </div>
    </div>

  <div>
    <% IngredientCategory.all.each do |category| %>
      <%= render 'shared/table_ingredients', category: category %>
    <% end %>
  </div>
</div>
<br>
