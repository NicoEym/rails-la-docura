<br>
  <div class="container">
    <h1 class="text-center">Cardapio</h1>
    <% if user_signed_in? %>
    <div class="row">
      <div class="button-box">
        <% if policy(@standard_cake).new? %>
          <%= link_to raw('<button type="button" class="btn btn-primary btn-chocolate-color chocolate-color-background">Criar um novo bolo</button>'), new_standard_cake_path(@standard_cake) %>
        <% end %>
      </div>
    </div>
    <% end %>

  <%= render 'shared/cardstandardcakes', standard_cake: @standard_cakes %>
  </div>
<br>
